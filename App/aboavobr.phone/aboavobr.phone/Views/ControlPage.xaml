<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converter="clr-namespace:aboavobr.phone.Views.Converter"
             x:Class="aboavobr.phone.Views.ControlPage">
   <ContentPage.Resources>
      <converter:InvertedValueConverter x:Key="InvertedValueConverter" />
   </ContentPage.Resources>
   
   <ContentPage.Content>
      <Grid>
         <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
         </Grid.RowDefinitions>
         <StackLayout Orientation="Horizontal" Grid.Row="0">
            <Label Text="ABOAVOBR" 
           HorizontalOptions="Start"
           VerticalOptions="StartAndExpand" />
            <Label Text="Battery:" />
            <Label Text="{Binding BatteryLifeInPercent}" />
         </StackLayout>
         <Image Grid.Row="1" Source="{Binding ImageSource}" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Aspect="AspectFill" IsVisible="{Binding IsCameraSupported}" />
         <Label Grid.Row="1" Text="Camera not Supported" HorizontalOptions="FillAndExpand" IsVisible="{Binding IsCameraSupported, Converter={StaticResource InvertedValueConverter}}" />
      </Grid>
   </ContentPage.Content>
</ContentPage>